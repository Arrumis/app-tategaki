# tategaki の基本設定です。

# Web アプリをホストで公開するポートです。
APP_PORT=3000

# 永続化データの保存先です。
HOST_DATA_DIR=./data
TATEGAKI_DATA_DIR=./data

# コンテナ内タイムゾーンです。
TZ=Asia/Tokyo

# 巡回ジョブの調整値です。
# 通常は既定値のままで構いません。
CRAWL_INTERVAL_MS=10800000
REQUEST_DELAY_MS=3000
